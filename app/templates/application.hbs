{{page-title "NaNoWriMo"  separator=" | "}}

{{#if is_authenticated}}
  <div class="page-background">
		{{#if show_navigation}}
    <header>
      {{#if (media "isMobile")}}
      <nav>
        {{nano-side-menu}}
      </nav>
      {{/if}}
      {{#if (media "isDesktop")}}
      <nav>
        {{micro-nav}}
      </nav>
      {{/if}}
      <nav>
        {{main-nav}}
      </nav>
      <NwUnverifiedHandler />
      <NwUnknownAgeHandler />
      {{#if UiOutdated}}
        <NwAlert @alertType="info">
          Application updates are available. <a {{action "reloadUI"}}>Reload application</a>
        </NwAlert>
      {{/if}}
    </header>
    {{/if}}
    <main class="logged-in 
			{{if currentUser.user.isNotConfirmed 'unverified' ''}} 
			{{if (not show_navigation) 'no-navigation'}}
			{{if currentUser.unknownBirthdate 'unknown-birthdate' }}
			">
      {{outlet}}
    </main>
    {{#if (and show_footer show_navigation) }}
      <footer>
        {{authenticated-footer}}
      </footer>
    {{/if}}
  </div>
{{else}}
  {{#if show_navigation}}
    <nav>
      {{logged-out-menu}}
    </nav>
  {{/if}}
  <main>
    {{outlet}}
  </main>
  {{#if show_navigation}}
    <footer>
      {{logged-out-footer}}
    </footer>
  {{/if}}
{{/if}}
