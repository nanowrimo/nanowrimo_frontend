<nav class="navbar bg-white">
  {{link-to 'NaNoWriMo' 'index' class="navbar-brand"}}

  <ul class="navbar-nav">
    <li class="nav-item">
      {{#if session.isAuthenticated}}
        <a href="#" class="nav-link" {{action 'invalidateSession'}}>Log out</a>
      {{else}}
        {{link-to 'Sign In' 'sign-in' class="nav-link"}}
      {{/if}}
    </li>
  </ul>
</nav>

<div class="container">
  <div class="row">
    <div class="col">
      {{outlet}}
    </div>
  </div>
</div>
