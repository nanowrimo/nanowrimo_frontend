<button class="nano-button-dark-blue full-width" {{action 'authenticateFacebook'}}>
  {{fa-icon "facebook-f"}} Continue with Facebook
  {{#if authFacebook}}
    <img class='spinner' src="/images/spinner.svg">
  {{/if}}
</button>
<button class="nano-button-red full-width" {{action 'authenticateGoogle'}}>
  {{fa-icon "google"}} Continue with Google
  {{#if authGoogle}}
    <img class='spinner' src="/images/spinner.svg">
  {{/if}}
</button>
