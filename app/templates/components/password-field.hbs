{{validated-input
  autocomplete="current-password"
  errors=changeset.errors
  hasAttemptedSubmit=hasAttemptedSubmit
  label="Password"
  name="password"
  type="password"
  value=changeset.password
}}
<button onclick={{action 'toggleVisible'}}>{{fa-icon "eye"}}</button>
