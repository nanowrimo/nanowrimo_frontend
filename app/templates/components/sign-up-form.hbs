{{#if isStepOne}}

  {{sign-up-form-step-one
    changeset=(changeset signUpAttempt SignUpAttemptPartOneValidations)
    submit=(action "step")
  }}

{{else}}

  {{sign-up-form-step-two
    changeset=(changeset signUpAttempt SignUpAttemptPartTwoValidations)
    submit=(action "submit")
  }}

{{/if}}
