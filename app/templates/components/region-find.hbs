<div class="nano-card g3-col1 g3-row1 g3-hspan1 g3-vspan2 g2-col1 g2-row1 g2-hspan1 g2-vspan2 wrap-content">
  <div class="nano-subcard">
    <div class="nano-subcard-face face-front filled-grey">
      <div class="nano-subcard-header">
        <div class="corner-label">My Regions</div>
      </div>
      <div class="nano-subcard-content no-padding" style="max-height: 300px;">
        <div class="nano-list-message">
          You haven't joined a region! Please select one of the regions from the list below.
          <br />
          
        </div>
        
        <ul class="nano-list">
          {{#each userRegions as |regionContainer index|}}
            {{group-list-item groupContainer=regionContainer listOrder=index limitList=limitList sortBySearch=sortBySearch}}
          {{/each}}
        </ul>
      </div>
    </div>
  </div>
  <div class="nano-subcard">
    <div class="nano-subcard-face face-front filled-grey">
      <div class="nano-subcard-header">
        <div class="corner-label">Find a Region</div>
        <div class="nano-subcard-tab {{searchTab}}" {{action 'findByString'}}>
          Search
        </div>
        <div class="nano-subcard-tab {{proximityTab}}" {{action 'getUserLocation'}}>
          Nearest to me
        </div>
      </div>
      <div class="nano-subcard-content no-padding">
        <div class="nano-list-message {{processingShow}}">
          Thanks for your patience while we determine your location...
        </div>
        <div class="nano-list-search {{proximityHide}}">
          {{input class="nano-list-search-field" placeholder="Search region names..." value=tempSearchString key-up="searchStringChange"}}
        </div>
        <ul class="nano-list {{processingHide}}" style="height: 700px; ">
          {{#each sortedRegions as |regionContainer index|}}
            {{group-list-item groupContainer=regionContainer listOrder=index limitList=limitList sortBySearch=sortBySearch statusChanged=(action "setRecomputeUserRegions")}}
          {{/each}}
        </ul>
      </div>
    </div>
  </div>
</div>
<div class="nano-card g3-col2 g3-row1 g3-hspan2 g3-vspan2 g2-col2 g2-row1 g2-hspan1 g2-vspan2">
  <div class="nano-subcard">
    <div class="nano-subcard-face face-front">
      <div class="nano-subcard-content no-padding">
        {{#g-map lat=center_latitude lng=center_longitude zoom=mapZoom as |context|}}
          {{#each sortedRegions as |region index|}}
             {{#if (lt index limitList)}}
               {{g-map-marker context lat=region.regionObject.latitude lng=region.regionObject.longitude}}
             {{/if}}
          {{/each}}
        {{/g-map}}
      </div>
    </div>
  </div>
</div>
