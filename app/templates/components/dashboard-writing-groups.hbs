<div class="nw-card-header">
  <div class="nw-card-title">Writing Groups</div>
</div>
<div class="nw-card-content">
  {{#if hasWritingGroup}}
    <div class='nw-column-cards'>
      {{#each writingGroups as |group|}}
        {{group-listing group=group buttons=true message=false isCurrentUser=true}}
      {{/each}}
    </div>
  {{else}}
    <div>
      <div>
        <div class="nw-promo-text">
          November is right around the corner! Create a writing group to navigate the challenge with friends!
        </div>
        <div>
          <button {{action "openNewWritingGroupModal"}}>
            Create a writing group
          </button>
          {{group-create-modal
            onHidden=(action "afterWritingGroupModalClose")
            open=addWritingGroup
            user=user
          }}
        </div>
      </div>
      <div class="nw-card-bottom-image"><img src="/images/cards/handsclapping.svg" style='width: 70px;' /></div>
    </div>
  {{/if}}
</div>